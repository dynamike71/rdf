# Find and remove duplicate files in all subdirectories
# Option to simulate process (--report-only) in order to just get the list of files to be removed and why they are listed. 
#
#    rdf.sh "*.txt" --report-only
